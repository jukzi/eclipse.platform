name: "CodeQL"

on:
  push:
    branches: [ "master" ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "master" ]
  schedule:
    - cron: '17 5 * * 6'

jobs:
  analyze:
    name: CodeQL2
    uses: jukzi/eclipse.platform.ui/.github/workflows/codeql.yml@master
